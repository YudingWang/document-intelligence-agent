services:
  api:
    build: ./backend
    env_file: .env
    environment:
      DATA_DIR: /data
    volumes:
      - qa-data:/data
    ports:
      - "8000:8000"

  web:
    build: ./frontend
    ports:
      - "5173:80"
    depends_on:
      - api

volumes:
  qa-data:
